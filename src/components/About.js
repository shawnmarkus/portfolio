import React from "react";
// import { useRef } from "react";

const About = ({ id }) => {
  return (
    <div id={id} style={{ height: "100vh" }}>
      About
    </div>
  );
};

export default About;
